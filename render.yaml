services:
  - type: web
    name: umrah-companion-api
    runtime: node
    plan: free
    rootDir: server
    buildCommand: npm install && npm run render:build
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        generateValue: true
